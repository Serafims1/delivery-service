[project]
name = "delivery-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standart]>=0.139.2",
    "pydantic-settings[yaml]>=2.14.2",
]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "httpx>=0.28.1",
    "mypy>=2.3.0",
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "radon>=6.0.1",
    "ruff>=0.16.0",
]
